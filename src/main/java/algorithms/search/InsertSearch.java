package algorithms.search;

/**
 * 插值查找， 要求：查询序列需要有序 且 元素分布均匀
 * 公式： int mid = left + (right - left) * (findValue -array[left]) / (array[right] - array[left])
 * 上面的公式得到的是 索引
 * 思路和 二分查找一致
 */
public class InsertSearch {


}
